#include "main.h"
/**
 * print_last_digit - prints last digit
 * of a number.
 *
 * @i: input an int
 *
 *Return: returns val of last digit
 */
int print_last_digit(int);
{
	int m;

	m = n % 10;
	if (m < 0)
	{
		_putchar(-m + 48);
		return (-l);
	}
	else
	{
		_putchar(l + 48);
		return (l);
	}
}
